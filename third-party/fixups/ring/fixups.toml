# ring build.rs compiles platform-specific C/assembly into a static archive; it must be linked.
[buildscript.run]
rustc_link_lib = true
rustc_link_search = true

[buildscript.run.env]
OPT_LEVEL = "0"
DEBUG = "false"
PROFILE = "release"
