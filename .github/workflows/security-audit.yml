# Daily check of cargo dependencies against the RustSec Advisory Database.
# Opens or updates a GitHub issue labeled "security" on failure and auto-closes
# it when all advisories clear. Delegates to the arcuru/actions reusable
# workflow.
name: "Deps: Security Audit"

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  audit:
    uses: arcuru/actions/.github/workflows/security-audit.yml@ec6897e9561bdefa62d5eb364d49b41bcba122a5 # v0.4.0
    with:
      runs-on: ubicloud-standard-2
      enable-magic-cache: "false"
      extra-substituters: https://cache.eidetica.dev
      extra-trusted-public-keys: cache.eidetica.dev-1:eND5gRJlbnool3ZLCWT2H8kkygWS8JcsU76HYXbWPBI=
